name: ops_calendar
description: A lightweight, performant Flutter month calendar with multi-day event ribbons. State-management agnostic.
version: 0.1.0
homepage: https://github.com/hariantara/ops_calendar
repository: https://github.com/hariantara/ops_calendar
issue_tracker: https://github.com/hariantara/ops_calendar/issues

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  very_good_analysis: ^6.0.0

flutter:
